export interface IProduct
{
    name?: string,
    description?: string,
    colors?: Array<any>,
    price?: number,
    quantity?: number,
    imgURL?: string,
    categoryID?: number,
    // id?: string
    _id?: string
  }